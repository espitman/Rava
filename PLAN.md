# برنامهٔ ساخت Rava Engine

هدف پروژه ساخت یک موتور محلی و بدون رابط چت است که اپ‌های اندرویدی شخصی بتوانند از یک قرارداد مشترک به نشست‌های وب ChatGPT و Gemini متصل شوند. نمونهٔ اولیه روی Termux و loopback اجرا می‌شود؛ پس از اثبات پایداری، همان قرارداد پشت Android Bound Service قرار می‌گیرد.

## اصول ثابت

- موتور فقط روی `127.0.0.1` گوش می‌دهد، مگر اینکه احراز هویت صریحاً فعال شود.
- شناسهٔ مدل‌ها نام‌فضادار است: `chatgpt/<model>` و `gemini/<model>`.
- مدل درخواستی باید واقعاً resolve و انتخاب شود؛ fallback بی‌صدا مجاز نیست.
- هر گفتگو به `app_id`، provider و مدل قفل می‌شود.
- نشست و صف اجرای ChatGPT و Gemini مستقل‌اند.
- رابط داخلی موتور از transport جدا می‌ماند تا HTTP بعداً با AIDL/Messenger جایگزین شود.

## فازها

- [x] فاز ۰ — اسکلت پروژه و قرارداد هسته
  - مدل: `نامشخص (خانوادهٔ GPT-5)`؛ ابزار: Codex
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - مدل: `gpt-6-astra`؛ ابزار: sub-agent همکاری
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - پکیج Python 3.11، تنظیمات محیطی، مدل داده و خطاها
  - registry آداپترها و routing مدل‌های نام‌فضادار
  - ذخیره و جداسازی conversationها بر اساس اپ

- [x] فاز ۱ — API محلی قابل‌آزمایش
  - مدل: `نامشخص (خانوادهٔ GPT-5)`؛ ابزار: Codex
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - `GET /health`
  - `GET /v1/models`
  - `POST /v1/conversations`
  - `POST /v1/chat/completions` با پاسخ عادی و SSE
  - لغو درخواست و حذف conversation

- [x] فاز ۲ — اتصال Gemini
  - مدل: `نامشخص (خانوادهٔ GPT-6)`؛ ابزار: Codex و ADB روی گوشی واقعی
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - آزمون واقعی کامل با profile جدا پاس شد. مرورگر اختصاصی Gemini هنگام استفاده
    باز می‌ماند؛ بستن آن در آزمون این دستگاه نشست را `UNAUTHENTICATED` کرد.
  - استفاده از `HanaokaYuzu/Gemini-API`
  - ورود با cookie، کشف پویای مدل‌ها و انتخاب fail-closed
  - ادامهٔ گفتگو، streaming و بازیابی خطاهای نشست
  - آزمون واقعی روی Termux/Android ARM64

- [x] فاز ۳ — اتصال ChatGPT
  - مدل: `نامشخص (خانوادهٔ GPT-6)`؛ ابزار: Codex و ADB روی گوشی واقعی
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - استفاده از `Octo-Lex/ChatGPT-Web2API` به‌عنوان sidecar مرورگر
  - fork محدود برای اجباری‌کردن موفقیت و تأیید انتخاب مدل
  - نگهداری conversation id و streaming
  - اجرای Chromium/CDP روی Termux/X11 و آزمون روی گوشی واقعی

- [ ] فاز ۴ — دوام و مشاهده‌پذیری
  - ذخیرهٔ امن metadata گفتگوها بدون ذخیرهٔ cookie در دیتابیس برنامه
  - health تفکیک‌شده، timeout، cancellation و backpressure
  - بازیابی پس از توقف process و منقضی‌شدن نشست

- [x] ابزار نصب گرافیکی Termux
  - مدل: `نامشخص (خانوادهٔ GPT-6)`؛ ابزار: Codex، Gradle و ADB روی گوشی واقعی
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - رابط انگلیسی مرحله‌به‌مرحله برای نصب، ورود، اجرا و بررسی وضعیت
  - انتقال bundle داخلی پروژه به Termux بدون نیاز به ADB یا دانلود پروژه
  - دریافت نتیجهٔ فرمان‌ها از `RunCommandService` و نمایش log داخل اپ
  - build و lint موفق و آزمون ارتباط واقعی با Termux روی گوشی

- [ ] فاز ۵ — بسته‌بندی اندروید
  - APK موتور با Bound Service
  - SDK کوچک Kotlin و callback استریم
  - signature permission برای محدودکردن دسترسی به اپ‌های شخصی
  - Activity موقت فقط برای ورود یا ترمیم نشست

## معیار پذیرش نمونهٔ اولیه

1. `GET /v1/models` مدل‌های واقعی هر حساب را با prefix درست برگرداند.
2. درخواست یک مدل ناموجود خطا بدهد و به مدل دیگری fallback نکند.
3. دو `app_id` نتوانند conversation یکدیگر را ادامه دهند.
4. پاسخ عادی و streaming هر دو کار کنند.
5. خاموش و روشن‌شدن یک provider باعث ازکارافتادن provider دیگر نشود.
